apiVersion: v1
kind: Service
metadata:
  name: sample-app-service
  namespace: production
spec:
  selector:
    app: sample-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
